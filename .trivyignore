# .trivyignore — accepted CVEs (one `CVE-YYYY-NNNN` per line, optional trailing comment).
#
# Usage: Trivy drops any finding listed here from the CI report, so use this
# file only for vulnerabilities you consciously accept (e.g. no upstream fix
# yet, or not exploitable in this deployment). Every entry MUST carry a short
# justification and, when possible, an expiry/review date.
#
# Example:
#   CVE-2024-12345  # nginx:1.29-alpine — no fixed release yet, re-check 2026-12-01
#
# No local Trivy run was available when this file was created, so it starts
# empty on purpose: run `trivy image <image>` (or the CI `scan` job), triage
# HIGH/CRITICAL findings, and add accepted CVEs here with justification.
# Follow-up: publish SARIF to GitHub code scanning (needs a separate job with
# `permissions: security-events: write`; current workflows keep `contents: read`).
#
# Triaged 2026-09-14 (trivy CLI, --severity HIGH,CRITICAL --ignore-unfixed):
# all digests below were the latest published digests on that date (resolved
# via `skopeo inspect docker://<image> | jq -r .Digest`), i.e. fixes exist in
# individual packages but no rebuilt image was published upstream yet. Every
# entry is re-checked automatically: the weekly Security cron re-scans, and
# Renovate bumps (tag or digest) change the image under test. Review date for
# all entries below: 2026-12-14.
#
# amazon/aws-cli was bumped 2.15.0 -> 2.36.44 instead (clean scan, no entries
# needed); bitnami/kubectl:latest@sha256:b29d8... scans clean.
#
# --- velero/velero-plugin-for-aws:v1.14.2@sha256:0751144c (initContainer) ---
# v1.14.2 is the newest plugin release; built with Go 1.25.11, fixes need an
# upstream rebuild with Go >= 1.25.12/1.26.5. InitContainer runs only at pod
# start to copy the plugin binary (no network service, minimal exposure).
CVE-2026-33818  # velero-plugin Go stdlib — fixed 1.25.13, needs upstream rebuild, re-check 2026-12-14
CVE-2026-39821  # velero-plugin Go stdlib — fixed 1.25.13, needs upstream rebuild, re-check 2026-12-14
CVE-2026-39822  # velero-plugin Go stdlib — fixed 1.25.12, needs upstream rebuild, re-check 2026-12-14
CVE-2026-46600  # velero-plugin golang.org/x/net — fixed 0.56.0, needs upstream rebuild, re-check 2026-12-14
CVE-2026-56852  # velero-plugin golang.org/x/text — fixed 0.39.0, needs upstream rebuild, re-check 2026-12-14
CVE-2026-56853  # velero-plugin Go stdlib — fixed 1.25.13, needs upstream rebuild, re-check 2026-12-14
CVE-2026-56858  # velero-plugin Go stdlib — fixed 1.25.13, needs upstream rebuild, re-check 2026-12-14
CVE-2026-56859  # velero-plugin Go stdlib — fixed 1.25.13, needs upstream rebuild, re-check 2026-12-14
CVE-2026-56860  # velero-plugin Go stdlib — fixed 1.25.13, needs upstream rebuild, re-check 2026-12-14
CVE-2026-56862  # velero-plugin Go stdlib — fixed 1.25.13, needs upstream rebuild, re-check 2026-12-14
CVE-2026-84304  # velero-plugin grpc — fixed 1.83.1, needs upstream rebuild, re-check 2026-12-14
CVE-2026-84445  # velero-plugin grpc — fixed 1.82.2/1.83.2, needs upstream rebuild, re-check 2026-12-14
GHSA-hrxh-6v49-42gf  # velero-plugin grpc — fixed 1.82.1, needs upstream rebuild, re-check 2026-12-14
#
# --- ghcr.io/gethomepage/homepage:v2.3.0@sha256:f8202766 (dashboard) ---
# v2.3.0 is the newest homepage release; Node.js/npm packages with fixes
# upstream but no rebuilt image published as of 2026-09-14. Build-time /
# bundled-web dependencies, not reachable as a network service at runtime.
CVE-2026-13149  # homepage brace-expansion — fixed 2.1.2, no rebuilt image, re-check 2026-12-14
CVE-2026-14257  # homepage brace-expansion — fixed 2.1.3, no rebuilt image, re-check 2026-12-14
CVE-2026-14456  # homepage libssl3 — fixed 3.5.8-r0, no rebuilt image, re-check 2026-12-14
CVE-2026-33671  # homepage picomatch — fixed 4.0.4, no rebuilt image, re-check 2026-12-14
CVE-2026-48815  # homepage sigstore — fixed 4.1.1, no rebuilt image, re-check 2026-12-14
CVE-2026-59873  # homepage tar CRITICAL — fixed 7.5.19, build-time dep, no rebuilt image, re-check 2026-12-14
CVE-2026-59874  # homepage tar — fixed 7.5.18, build-time dep, no rebuilt image, re-check 2026-12-14
CVE-2026-69152  # homepage brace-expansion — fixed 2.1.4, no rebuilt image, re-check 2026-12-14
CVE-2026-69192  # homepage ip-address — fixed 10.3.1, no rebuilt image, re-check 2026-12-14
CVE-2026-73566  # homepage tar — fixed 7.5.21, build-time dep, no rebuilt image, re-check 2026-12-14
CVE-2026-9496  # homepage pacote — fixed 21.5.1, build-time dep, no rebuilt image, re-check 2026-12-14
GHSA-rgj7-g3m4-5g8c  # homepage sharp — fixed 0.35.4, no rebuilt image, re-check 2026-12-14
