apiVersion: v1
kind: ConfigMap
metadata:
  name: vault-setup-script
  namespace: vault
  annotations:
    argocd.argoproj.io/hook: PreSync
data:
  setup-vault.sh: |
{{ .Files.Get "scripts/setup-vault.sh" | indent 4 }}
