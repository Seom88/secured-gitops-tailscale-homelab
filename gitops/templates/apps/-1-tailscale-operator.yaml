{{- if .Values.platformApps.enabled }}
# -1 breaks 00- prefix — wave -1 must sort before 00-* so operator gates waves 0..4
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: tailscale-operator{{- if .Values.developmentApp.enabled }}-dev{{- end }}
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
    argocd.argoproj.io/tracking-id: gitops:argoproj.io/Application:argocd/tailscale-operator
  labels:
    wave-policy: healthy
spec:
  project: default
  source:
    repoURL: {{ .Values.repoURL }}
    targetRevision: {{ if .Values.developmentApp.enabled }}dev{{ else }}main{{ end }}
    path: platform/tailscale-operator
    helm:
      ignoreMissingValueFiles: true
      valueFiles:
        - values.yaml
        {{- if .Values.developmentApp.enabled }}
        - values-dev.yaml
        {{- end }}
  destination:
    server: https://kubernetes.default.svc
    namespace: tailscale
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - Retry=true
{{- end }}
